<Window x:Class="InventoryApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:InventoryApp"
        xmlns:vm="clr-namespace:InventoryApp.ViewModels"
        mc:Ignorable="d"
        Title="{Binding Title}" Height="800" Width="1200"
        WindowStartupLocation="CenterScreen">
    
    <Window.Resources>
        <!-- Local Menu Button Style -->
        <Style x:Key="MenuButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Height" Value="45"/>
            <Setter Property="Margin" Value="10,2"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="15,0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="6" 
                                Margin="{TemplateBinding Margin}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                            VerticalAlignment="Center"
                                            Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#4A5F7A"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#5D6D7E"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <!-- Left Navigation Panel - Full Height -->
        <Border Grid.Column="0" Background="#34495E" Padding="0">
            <StackPanel>
                <TextBlock Text="📱 ENVANTER YÖNETİMİ" 
                           FontSize="16" FontWeight="Bold" 
                           Foreground="White" 
                           HorizontalAlignment="Center"
                           Margin="0,20,0,30"
                           TextWrapping="Wrap"/>
                
                <Button x:Name="AnasayfaButton" 
                        Content="🏠 Anasayfa" 
                        Style="{StaticResource MenuButtonStyle}"
                        Click="MenuButton_Click" Tag="Anasayfa"/>
                
                <Button x:Name="MuhasebeButton" 
                        Content="💼 Muhasebe" 
                        Style="{StaticResource MenuButtonStyle}"
                        Click="MenuButton_Click" Tag="Muhasebe"/>
                
                <Button x:Name="MusterilerButton" 
                        Content="👥 Müşteriler" 
                        Style="{StaticResource MenuButtonStyle}"
                        Click="MenuButton_Click" Tag="Musteriler"/>
                
                <Button x:Name="StokButton" 
                        Content="📦 Stok" 
                        Style="{StaticResource MenuButtonStyle}"
                        Click="MenuButton_Click" Tag="Stok"/>
                
                <Button x:Name="SatislarButton" 
                        Content="💰 Satışlar" 
                        Style="{StaticResource MenuButtonStyle}"
                        Click="MenuButton_Click" Tag="Satislar"/>
                
                <Button x:Name="IstatistiklerButton" 
                        Content="📊 İstatistikler" 
                        Style="{StaticResource MenuButtonStyle}"
                        Click="MenuButton_Click" Tag="Istatistikler"/>
                
                <Button x:Name="AyarlarButton" 
                        Content="⚙️ Ayarlar" 
                        Style="{StaticResource MenuButtonStyle}"
                        Click="MenuButton_Click" Tag="Ayarlar"/>
                
                <!-- Statistics at bottom of menu -->
                <StackPanel Margin="15,40,15,20">
                    <Border Background="#3498DB" CornerRadius="8" Padding="15,10" Margin="0,0,0,10">
                        <StackPanel>
                            <TextBlock Text="� Toplam Değer" Foreground="White" FontWeight="Bold" FontSize="12"/>
                            <TextBlock Text="{Binding TotalInventoryValue, StringFormat='{}{0:C}'}" 
                                       Foreground="White" FontSize="16" FontWeight="Bold"/>
                        </StackPanel>
                    </Border>
                    
                    <Border Background="#E74C3C" CornerRadius="8" Padding="15,10">
                        <StackPanel>
                            <TextBlock Text="⚠️ Düşük Stok" Foreground="White" FontWeight="Bold" FontSize="12"/>
                            <TextBlock Text="{Binding LowStockCount}" 
                                       Foreground="White" FontSize="16" FontWeight="Bold"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </StackPanel>
        </Border>
        
        <!-- Right Content Area -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!-- Header with Search and User Profile -->
            <Border Grid.Row="0" Background="#F8F9FA" BorderBrush="#E9ECEF" BorderThickness="0,0,0,1" Padding="20,10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="350"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <!-- Page Title -->
                    <TextBlock Grid.Column="0" x:Name="PageTitleText"
                               Text="🏠 Anasayfa" 
                               FontSize="20" 
                               FontWeight="Bold" 
                               Foreground="#2C3E50"
                               VerticalAlignment="Center"
                               Margin="0,0,20,0"/>
                    
                    <!-- Search Box with Embedded Button -->
                    <Border Grid.Column="1" BorderBrush="#BDC3C7" BorderThickness="2" CornerRadius="20" Background="White">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="40"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            
                            <!-- Search Button Inside -->
                            <Button Grid.Column="0" 
                                    Content="🔍" 
                                    Command="{Binding SearchProductsCommand}"
                                    Background="Transparent" 
                                    BorderThickness="0"
                                    FontSize="16"
                                    Cursor="Hand"
                                    Margin="5,5,0,5"/>
                            
                            <!-- Search TextBox -->
                            <TextBox Grid.Column="1"
                                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                                     Height="40" 
                                     VerticalContentAlignment="Center"
                                     FontSize="14"
                                     BorderThickness="0"
                                     Background="Transparent"
                                     Margin="0,5,10,5">
                                <TextBox.Style>
                                    <Style TargetType="TextBox">
                                        <Style.Triggers>
                                            <Trigger Property="Text" Value="">
                                                <Setter Property="Background">
                                                    <Setter.Value>
                                                        <VisualBrush AlignmentX="Left" AlignmentY="Center" Stretch="None">
                                                            <VisualBrush.Visual>
                                                                <TextBlock Text="Tüm sayfalarda arama..." Foreground="#7F8C8D" FontSize="14" Margin="5,0"/>
                                                            </VisualBrush.Visual>
                                                        </VisualBrush>
                                                    </Setter.Value>
                                                </Setter>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBox.Style>
                                <TextBox.InputBindings>
                                    <KeyBinding Key="Enter" Command="{Binding SearchProductsCommand}"/>
                                </TextBox.InputBindings>
                            </TextBox>
                        </Grid>
                    </Border>
                    
                    <!-- User Profile Section -->
                    <Border Grid.Column="3" Background="#ECF0F1" CornerRadius="20" Padding="10,5">
                        <Button x:Name="UserProfileButton" 
                                Background="Transparent"
                                BorderThickness="0"
                                Cursor="Hand"
                                Click="UserProfileButton_Click">
                            <StackPanel Orientation="Horizontal">
                                <!-- Profile Photo -->
                                <Border Width="30" Height="30" CornerRadius="15" Background="#3498DB" Margin="0,0,10,0">
                                    <TextBlock Text="👤" FontSize="16" 
                                               HorizontalAlignment="Center" 
                                               VerticalAlignment="Center"
                                               Foreground="White"/>
                                </Border>
                                
                                <!-- User Name -->
                                <StackPanel VerticalAlignment="Center">
                                    <TextBlock x:Name="UserFullNameText" 
                                               Text="{Binding CurrentUser.FullName}" 
                                               FontWeight="Bold" 
                                               FontSize="13"
                                               Foreground="#2C3E50"/>
                                    <TextBlock x:Name="UserEmailText" 
                                               Text="{Binding CurrentUser.Email}" 
                                               FontSize="11"
                                               Foreground="#7F8C8D"/>
                                </StackPanel>
                                
                                <!-- Dropdown Arrow -->
                                <TextBlock Text="▼" FontSize="10" 
                                           VerticalAlignment="Center" 
                                           Margin="8,0,0,0"
                                           Foreground="#7F8C8D"/>
                            </StackPanel>
                        </Button>
                    </Border>
                    
                    <!-- User Menu Popup -->
                    <Popup x:Name="UserMenuPopup" 
                           PlacementTarget="{Binding ElementName=UserProfileButton}"
                           Placement="Bottom"
                           StaysOpen="False"
                           AllowsTransparency="True">
                        <Border Background="White" 
                                BorderBrush="#BDC3C7" 
                                BorderThickness="1" 
                                CornerRadius="8"
                                Margin="5">
                            <Border.Effect>
                                <DropShadowEffect Color="Black" Opacity="0.2" BlurRadius="10" ShadowDepth="2"/>
                            </Border.Effect>
                            <StackPanel MinWidth="150">
                                <Button x:Name="LogoutButton" 
                                        Content="🚪 Çıkış Yap"
                                        Background="Transparent"
                                        BorderThickness="0"
                                        HorizontalAlignment="Stretch"
                                        HorizontalContentAlignment="Left"
                                        Padding="15,10"
                                        FontSize="13"
                                        Cursor="Hand"
                                        Click="LogoutButton_Click">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#F8F9FA"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>
                            </StackPanel>
                        </Border>
                    </Popup>
                </Grid>
            </Border>
            
            <!-- Main Content Area -->
            <Border Grid.Row="1" Margin="20">
                <ContentPresenter x:Name="MainContentPresenter" 
                                  Content="{Binding CurrentView}"/>
            </Border>
            <!-- Status Bar -->
            <StatusBar Grid.Row="2" Background="#BDC3C7">
                <StatusBarItem>
                    <TextBlock Text="{Binding StatusText}"/>
                </StatusBarItem>
                <Separator/>
                <StatusBarItem>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Durum: "/>
                        <TextBlock Text="{Binding IsBusy, Converter={x:Static local:BoolToStringConverter.Instance}, ConverterParameter='Yükleniyor...|Hazır'}"/>
                    </StackPanel>
                </StatusBarItem>
            </StatusBar>
        </Grid>
    </Grid>
</Window>
